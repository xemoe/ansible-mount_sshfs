---
ssh_config_dir: /opt/ssh_config
ssh_config_file: "{{ ssh_config_dir }}/config"
ssh_config_debug: true
ssh_config_hosts:
  mainbox:
    name: mainbox
    hostname: 172.100.0.200
    port: 22
    username: vagrant
    identity: id_rsa
    sshpass: vagrant
    active: true
